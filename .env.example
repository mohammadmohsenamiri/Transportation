# متغیرهای محیطی نمونه — این فایل commit می‌شود؛ .env واقعی هرگز commit نشود.
# در Phase 0 هیچ اتصال پایگاه‌داده یا سرویس خارجی وجود ندارد.

NODE_ENV=development

# از Phase 1 به بعد: DATABASE_URL، متغیرهای session/auth و غیره اینجا اضافه می‌شوند.
